1. Creating a new store fills it with invalid products (probably an uncleaned testing), inexistent products gets created. — [DONE: removed Qwen's auto-seed-products step from onboarding; new stores start empty]
2. No loading state or store morph for new adding products, the merchant does not know if qwen is adding or doing anything till it is done — [DONE: photo-drop vision batch is now chunked (calls Qwen in rounds instead of one giant blocking request), shows a live "N/M catalogued" counter instead of a static message, sets expectations about how long it can take, and disables/warns on navigating away mid-process so it's never ambiguous whether Qwen is still working]
3. Double drop bar, I think we can consolidate to one and merge the two to take usable formats — [DONE: one drop zone now, detects CSV vs photos from the dropped file(s) and routes to the right existing pipeline; a mixed drop runs CSV import first then photo vision]
4. I think merchant should have the right to Edit what qwen wrote if they wanna and qwen should take note of the change and reasonableness and learn — [CORRECTION + DONE: I was wrong earlier saying this "already works" — the PATCH endpoint + memory-write existed, but the ONLY frontend caller was CatalogReview, which only edits products already flagged with a data-quality issue during onboarding. A normal, unflagged product had zero edit affordance anywhere, onboarding or post-publish. Added a real "Edit" toggle to every product card on /products (reachable post-publish via the terminal's "products" link too) wired to the existing PATCH endpoint, plus a visible "Qwen noted this change" confirmation on save so the memory-write is no longer invisible.]
5. Added 30 products, "C:\Users\kexin\Pictures\Screenshots\Screenshot (237).png" and scrolled down (the scrolling is kinda messy, shows blank then shows, hope it is not the code, maybe its nextjs idk or my machine) anyways it wrote publish 21 products, why the mismatch? "C:\Users\kexin\Pictures\Screenshots\Screenshot (238).png" — [PARTIAL: real cause was item 2 (no "still working" signal), not silent deletion — will resolve once that's fixed. Separately hardened the dedup logic (item 3 topic) so same-image-but-different-name products are never auto-merged, regardless.]
6. One big ass issue, images are not smart, I think we can either do that or qwen look "C:\Users\kexin\Pictures\Screenshots\Screenshot (239).png" — [DONE: ProductDetail.tsx now falls back to the branded tile on a dead image_url instead of a black box]
7. Qwen is not catching broken or imageless links! We need to improve the image vision or turn it to a more elaborate feature including the fingerprinting — [DONE: dead/non-image URLs are now caught before a Qwen call is even spent (skip straight to failed_urls); also fixed `confident` defaulting to True on a malformed/incomplete Qwen response — it now defaults to False so ambiguous vision results always go to merchant review instead of silently going live]
8. Allow merchants to be able to override % discounts or time discounts from qwen suggestion and qwen learns when and why gradually. — [DONE: pending discount cards (flash_sale/scarcity_price/recovery_offer/cart_dwell_nudge) now show an editable % input pre-filled with Qwen's proposal — approving with an edited value overrides it, still re-clamped by the interceptor server-side, and is written to qwen_memory (action_type=qwen_discount_override) so future proposals can learn from the correction. Also added the settings panel from item 10 for setting the ceiling directly.]
9. So, basically is qwen internal promo for attribution working? Cus users can actually buy and the promo is still active! which means they get 10% off so far they add to cart! poor! is that right? — [DONE (cart_dwell_nudge half): confirmed real bug — cart_dwell_nudge (the "still deciding, 8% off" nudge for a genuinely dwelling cart) was writing into the same global field as recovery_offer, so ANY session checking out during its window got the discount, not just the one that dwelled. Fixed with a per-session Redis key (Keys.dwell_offer) + a shared get_effective_discount() used by both cart display and checkout math — checkout previously re-derived the discount independently of the cart, which was the actual root cause. recovery_offer (the store-wide abandon-rate-spike offer) stays store-wide by design per our discussion — it has no single triggering session to scope to. OPEN: the residual gaming exposure on recovery_offer specifically — two options written up in the plan doc, not yet decided.]
10. Is Qwen limited to 10% off? it should be able to set dynamic discount 5% to whatever. — [DONE: confirmed real gap, not a hard cap — `max_discount_percent` already defaults to 40% (merchant-configurable), but `decision_engine.DECISION_PROMPT` never told Qwen what its actual ceiling was, so it was guessing blind and converging on a safe-sounding generic 10% (matches the exact finding already in BENCHMARKS.md's "subconscious vs. without one" section, which never made it back into the live prompt as a fix). Now `compose_decision_prompt` takes `max_discount_percent` and `run_decision_cycle` loads the merchant's real `BusinessConstraints` and passes it in, so Qwen reasons within its actual headroom instead of defaulting. Interceptor still re-checks and clamps regardless — this is a better-informed proposal, not a loosened guardrail.]
11. Categories could work better.  — [DONE: found the bug — chips/filters/related-products all matched category by exact case+whitespace-sensitive string, so "Shoes"/"shoes"/" Shoes " fragmented into separate non-overlapping chips. Normalized comparison everywhere (shared lib/category.ts helper) while keeping the original display casing.]
12. Is there a way to cache images using the links cus tf — [DONE: CSV/manual product image_urls now get mirrored to Elevate's own OSS bucket in the background right after the product is created — fetched once, re-hosted permanently, product's image_url flips to the new aliyuncs.com URL within a few seconds. Verified end-to-end against the real OSS bucket. Vision-batch photos already skipped this problem (uploaded to OSS at drop-time already); a dead/unreachable source URL just leaves the original in place, same as before.]
13. Should we upgrade the terminal to also show approval for products cus it being on the product page doesn't give room to seeit